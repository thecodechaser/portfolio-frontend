// initial-state
const initialState = []

// reducer
const repliesReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default repliesReducer;